# TLS를 활성화할 때 설치 도구가 두 placeholder를 검증된 절대 경로로 치환합니다.
# Edge는 private key 원본을 직접 소유하지 않고 service credential만 읽습니다.
[Service]
LoadCredential=tls-cert.pem:@CERT_FILE@
LoadCredential=tls-key.pem:@KEY_FILE@
