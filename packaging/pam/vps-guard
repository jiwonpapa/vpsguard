# SEC-015: PAM verifies only the server password and account state.
# The root helper independently enforces sealed TOTP or one-time recovery codes.
auth    requisite pam_succeed_if.so uid >= 1000 quiet
auth    required  pam_unix.so
account required  pam_unix.so
