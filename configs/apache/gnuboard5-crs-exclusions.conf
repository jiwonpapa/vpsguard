# SEC-017: intentionally empty until a concrete CRS rule ID is proven as a GnuBoard false positive.
# Never disable an entire CRS category; add narrow route/parameter scoped exclusions with evidence.
